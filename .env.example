# Checkend Community Edition Configuration
# Copy this file to .env and fill in the values.

# =============================================================================
# REQUIRED
# =============================================================================

# Secret key for encrypting sessions and cookies.
# Generate with: openssl rand -hex 64
SECRET_KEY_BASE=replace_me_with_64_char_hex_string

# PostgreSQL password for the database.
# Generate with: openssl rand -hex 32
POSTGRES_PASSWORD=replace_me_with_secure_password

# =============================================================================
# OPTIONAL
# =============================================================================

# Your domain for automatic Let's Encrypt SSL certificates.
# Only needed if exposing ports 80/443 directly (see compose.override.yml).
# THRUSTER_TLS_DOMAIN=checkend.example.com
